#!/bin/sh
# Remove committed node_modules and push commit
git rm -r --cached node_modules || true
git add .gitignore
git commit -m "Remove committed node_modules and ignore it" || true
git push || true